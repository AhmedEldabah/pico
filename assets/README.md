# Image assets

Drop the real PICO photos here with these exact filenames and they'll appear
automatically (each slot has a graceful fallback until the file exists):

| Filename            | Used for                          | Ideal format / size            |
|---------------------|-----------------------------------|--------------------------------|
| `founder.jpg`       | Heritage section portrait         | Dr. Kamel archival photo, ~900×1100 |
| `prod-honey.png`    | Value-Add card — honey            | Background-removed PNG, ~400×400 |
| `prod-frozen.png`   | Value-Add card — frozen fruit     | Background-removed PNG, ~400×400 |
| `prod-corn.png`     | Value-Add card — sweet corn       | Background-removed PNG, ~400×400 |
| `prod-jam.png`      | Value-Add card — honey jams       | Background-removed PNG, ~400×400 |

Optional (wire these up in `index.html` when ready):
- `pico-logo.png` — the official 674×347 logo from picoagriculture.com/images/pico.png
- Field / worker / avocado photography for future sections.

The current logo in the header is a hand-built **SVG** recreation (crimson gradient
wordmark + green leaf), so it stays crisp at any size and needs no file.
